import React from 'react'
import './Footer.css'


const Header = (props) => {
  return(
    <div>
    
    <div className="footer">{props.footerTitle}</div>
    
    </div>

  )
}

export default Header;